# webrtc-audio-examples

### Building from the command line

```console
cd <source root>
meson setup builddir
ninja -C builddir
```

### Building with the Visual Studio IDE

```console
meson build --backend=vs
```
